<div class="center jumbotron">
  <div class="text-center">
    <h1>ようこそ、タスク一覧へ</h1>
    <h2>welcom to the tasklist !!</h2>
  </div>
</div>

<ul>
  <% @tasks.each do |task| %>
    <li> <%= task.status %> &gt;<%= link_to task.content, task %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>